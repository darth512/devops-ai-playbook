* text=auto

# Shell scripts must keep LF endings — they run inside Linux containers
# (e.g. postgres docker-entrypoint-initdb.d scripts), and a Windows
# checkout with core.autocrlf=true converting them to CRLF breaks the
# shebang line ("/bin/bash^M: bad interpreter").
*.sh text eol=lf
